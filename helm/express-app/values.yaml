replicaCount: 2

image:
  repository: admintita/express-app
  tag: <git-sha>
service:
  type: NodePort
  port: 80
  targetPort: 3000
  nodePort: 30080

ingress:
  enabled: true
  className: nginx
  host: express-app.local
  path: /
  pathType: Prefix